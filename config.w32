// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("iplookup", "for iplookup support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("iplookup", "enable iplookup support", "no");

if (PHP_IPLOOKUP != "no") {
	EXTENSION("iplookup", "iplookup.c");
}

